spring:
  datasource:
    password: admin
    url: jdbc:postgresql://localhost:5432/spring_toys
    username: postgres

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
